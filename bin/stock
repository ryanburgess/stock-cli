#!/usr/bin/env node
var argument = process.argv[2];
var stock = require('../index');

switch (true) {
  case (argument !== undefined):
    stock(argument);
  break;
  default:
    console.log('stock [symbol] - Display stock info');
  break;
}
